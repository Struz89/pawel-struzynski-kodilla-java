package com.kodilla.good.patterns.challenges.food2door;

public class ProductOrderRepository implements OrderRepository{

    @Override
    public void createOrder(Producer producer, Integer quantity, String dateTime) {
    }
}
